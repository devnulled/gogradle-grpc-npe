plugins {
    id 'com.github.blindpirate.gogradle' version '0.9'
}

golang {
    packagePath = 'mycode.devnulled.com/' // go import path of project to be built, NOT local file system path!
}

dependencies {
    golang {
      build(['name':'google.golang.org/grpc'])
    }
}